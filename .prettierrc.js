module.exports = {
  // Semicolons at the end of statements (true: add semicolons, false: omit semicolons)
  semi: false,
  // Use single quotes instead of double quotes for strings (true: use single quotes, false: use double quotes)
  singleQuote: true,
  // Include parentheses around a sole arrow function parameter (always: always include, avoid: only add when necessary)
  arrowParens: 'avoid',
};
